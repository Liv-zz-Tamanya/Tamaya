
  # Emotion Diary Service

  This is a code bundle for Emotion Diary Service. The original project is available at https://www.figma.com/design/Z1XApMESfongrM9zZpLCP1/Emotion-Diary-Service.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  